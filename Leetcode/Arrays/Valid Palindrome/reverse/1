class Solution:
    def isPalindrome(self, s: str) -> bool:
        s=s.lower()
        string=""
        for i in s:
            if i.isalnum()==True:
                string+=i
        if string.lower() == string[::-1].lower():
            return True
        else:
            return False
                
